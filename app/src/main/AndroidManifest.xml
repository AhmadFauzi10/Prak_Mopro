<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ahmad.makananUts">

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="UTS Makanan"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name="com.ahmad.makananUts.MainActivity" android:screenOrientation="locked"/>
        <activity android:name="com.ahmad.makananUts.login" android:screenOrientation="locked"
            tools:ignore="GoogleAppIndexingWarning">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>